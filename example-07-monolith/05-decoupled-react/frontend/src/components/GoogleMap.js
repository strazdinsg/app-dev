/**
 * Well, this should be our Google map of the place... :)
 * @return {JSX.Element}
 * @constructor
 */
export function GoogleMap() {
  return (
    <p>
      P.S. Integrating Google Maps in a React app is not as straight-forward as
      just copying the embedding code. Want to have a map here? Find it out
      yourself ;)
    </p>
  );
}
